# englishstudy1

一个适合小孩使用的单文件背单词网页。

## 特点

- 单文件 `index.html`，不需要构建
- 默认先隐藏中文，适合测试记忆
- 支持分类、搜索、随机抽词、小测验
- 使用 Chrome 内置 `speechSynthesis` 做英文发音

## 使用

直接打开 `index.html` 即可，或者部署到 GitHub Pages。

## 在线访问

启用 GitHub Pages 后可通过仓库对应页面访问。
